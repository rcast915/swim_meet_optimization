# Roster class
# Wraps a list of Swimmers, tracks event assignments per swimmer
# Methods: get_eligible_swimmers(), action_mask(), assign(), events_since_last(), reset(), to_status_array()
# action_mask must filter by age_group + gender + event count limits
